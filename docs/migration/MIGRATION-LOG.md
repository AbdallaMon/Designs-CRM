# Migration Log — What's Done & Changed

> Live record of the executed migration (distinct from the *plans* in 01–07).
> Open this + `PROJECT_STATE.md` to see exactly what has been refactored and what remains.
> Branch: `server-migration` · Baseline (rollback point): commit `9406978` ("merged").

## Execution order (decided)

1. **Foundation** — monorepo skeleton + `packages/db` (frozen schema + shims) + `packages/shared` + `server/src` skeleton; app keeps booting.
2. **Infra** — relocate & de-dup v2 infra → `server/src/infra`: prisma, upload (local provider, **chunk mechanism unchanged**), redis (consolidate `ioredis`/`redis`), socket, telegram (single GramJS owner), mail, **workers from server bootstrap**, cron. Drive = dead/schema-only → left untouched.
3. **Auth + permissions** — unify JWT, permission codes + role profiles + scope checkers, `auth/me` emits real `permissions[]`. (Blocks all module work.)
4+. **Modules** — per module a backend+frontend pair (parallel) then a reconciliation review; independent modules run as parallel pairs. Order follows `07-decisions-resolved.md` §5.
**Final** — vitest suite green + build green → commit + stop. (No UI/UX redesign yet.)

## Guardrails (enforced on every agent)

- 🔒 PDF generation logic-frozen (split only). 🔒 Prisma schema frozen. Same observable API behavior.
- Single Arabic UI (no i18n toggle); keep message-code indirection.
- Upload **chunk mechanism must not change**; storage = local provider only for now.
- Workers run from the server bootstrap only.
- App must keep booting at every checkpoint (legacy + v2 coexist until cutover).

---

## Status

| Stage | Status | Notes |
|---|---|---|
| Checkpoint commit | ✅ done | baseline `9406978` + planning docs + WIP committed before structural changes (`5b9cc45`) |
| 1. Foundation | ✅ done & verified | monorepo + `@dms/db` (frozen schema) + `@dms/shared` seed + shims + vitest; app boots |
| 2. Infra | ✅ done & verified | **2a relocate v2→src + de-dup ✅**; **2b redis consolidation + workers/cron from bootstrap ✅ done & verified** (upload already local-only from 2a) |
| 3. Auth + permissions | ✅ done & verified | code-defined permission map (frozen schema has NO permission tables); unified JWT (read-shim for legacy cookie); `requirePermissions` + scope-checker + capabilities patterns; `auth/me` emits `permissions[]`/`permissionsByModule{}`; 5 v2 modules re-gated; 25 vitest tests green |
| 4+. Modules | ⏳ in progress | **Chat ✅**, **site-utility ✅**, **Courses/LMS ✅ (BE)**, **Leads/clientLead CORE ✅ (BE, IDOR keystone)**, **Users ✅ (BE)**, **Projects domain ✅ (BE: project+task+update+delivery)**, **Accounting ✅ (BE: payment+expense+note+rent+salary+report)**, **Calendar ✅ (BE: availability+google-oauth+public client-booking)**, **Notifications+Utilities ✅ (BE: notifications IDOR-fixed + lookup helpers)**, **Dashboard ✅ (BE: 9 role-scoped aggregations, IDOR-hardened)**, **Leaf domains ✅ (BE: questions+sales-stages+reviews, lead-scope IDOR-fixed)**, **Contracts ✅ (BE: authed CRUD + public e-sign; PDF wrapped/frozen; IDOR+SSRF fixed)** — done & verified, review (+rework) each. **435 tests**. IDOR/security holes fixed in every module (Courses 2 critical; Leads 2 HIGH; Users profile-IDOR+mass-assign; Projects broad-delete+PII+mass-assign; Accounting money-validation + dropped client-trusted oldPaymentLevel; Calendar SAFE/0-introduced, 3 ported quirks → backlog; Notifications UNAUTH cross-user IDOR + HIGH user-logs IDOR + open prisma[model] read locked; Dashboard cross-user metric over-exposure closed; Leaf domains questions/sales-stages unscoped lead data → reads access-scope + writes mutate-scope, reviews token-leak closed; Contracts unscoped contract IDOR → lead-scope via :contractId→lead + public e-sign SSRF on signatureUrl locked in validation, PDF only wrapped). Shared: validate middleware emits VALIDATION_ERROR code. Next: image-sessions[🔒PDF+🔒upload], admin/staff residual, + deferred clients public surface + FE |
| Final (tests + build + commit) | ⬜ not started | |

---

## Changelog (most recent first)

### Stage 4 — Contracts domain backend (authed CRUD + public e-sign; 🔒 PDF wrapped) — ✅ 2026-06-07
**Backend-only (FE deferred).** Migrated the contracts domain into v2 (strangler): **contract/** ← `routes/contract/contracts.js` (22 ep, SHARED gate) → `/v2/contracts` (authed staff/admin CRUD: contracts, drawings, stages, payments, special-items, cancel, pdf-session-token, grouped payments); **client/** ← `routes/contract/client-contract.js` (3 ep, PUBLIC token-based) → `/v2/client/contracts` (e-sign: GET /session, PUT /session/status, POST /generate-pdf). The public flow stays PUBLIC (no auth/code) — the per-session `Contract.arToken` (UUIDv4) IS the authentication, like `/v2/client/calendar`. **🔒 PDF LOGIC-FROZEN — wrapped only:** the frozen `buildAndUploadContractPdf` (`services/main/contract/generateContractPdf.js`) + `markContractAsCancelled`/`generatePdfSessionToken` are invoked ONLY via lazy adapters; no PDF logic/fonts/output/`__dirname` font-loading touched (confirmed no frozen file modified). **SECURITY — review verdict SAFE; one HIGH reworked before commit:** *IDOR (authed)* — legacy had NO object scope; every authed route now runs the leads keystone checker BEFORE the service call (`:leadId` direct; `:contractId` + child-ids `:paymentId/:stageId/:drawId/:itemId` resolve child→contract→`clientLeadId` in a repo then check); READS `checkIfUserCanAccessLead`, WRITES `checkIfUserCanMutateLead` (owned-only); create mutate-scopes the body `clientLeadId`; forged id→404 no leak; `GET /payments/all` keeps in-service per-role scoping (admin all / others `clientLead.userId===req.auth.id`, identity from `req.auth`). *Public e-sign IDOR closed* — session derived SOLELY from the verified token; `.strict()` bodies reject a client-supplied `id`/`contractId` (legacy `/session/status` accepted a raw `id` to target an arbitrary session). *SSRF (HIGH, fixed)* — `POST /generate-pdf` passes `signatureUrl` into the frozen builder's `fetch(${IMAGEDOMAIN}${signatureUrl})` raw concat; was `string().min(1)` → a token holder could drive an arbitrary server-side fetch. Locked in the v2 validation layer (no frozen change) to a safe relative upload path `^/[A-Za-z0-9_\-./]+\.(png|jpe?g|webp)$` + refine rejecting `..`/`://`/leading `//`/`@` — verified against the real signature shape (chunk-upload returns `/uploads/<uuid>.<ext>`, FE submits it verbatim). *sessionStatus* enum-locked to `ContractSessionStatus {INITIAL,SIGNING,REGISTERED}` (clean 422, no behavior change). *Money* coerced finite ≥0; `.strict()` bodies; path ids authoritative. **Ported quirks preserved (documented, not changed):** public e-sign state machine has no transition/replay guard (token holder can re-finalize / move status within the enum) — legacy behavior, follow-up decision; frozen `getContractSessionByToken` throws a plain Error→generic 500 on a bad token. **Authorization:** `CONTRACT_PERMISSIONS` (11 codes, reads/writes split) granted via `SHARED_AUTHED` = all 9 authed roles (matches legacy SHARED gate; not widened). **Contract:** envelope + `contractsMessages` (language-neutral codes — replaced legacy Arabic/English prose incl. `"تم حفظ الاستجابة بنجاح"`). **RENAMES (FE repoint):** `PATCH /:contractId/cancel`→`POST /:contractId/actions/cancel`; `PATCH /:contractId` (token)→`POST /:contractId/actions/generate-pdf-token`; payment status/amounts→`POST .../actions/change-status|update-amounts`. **Verified:** root `npm test` green — **435 tests / 28 files** (+37 incl. contract lead-scope allow-vs-deny with `:contractId→lead` resolution, public-signing token-cannot-be-overridden, SSRF payloads rejected + legit `/uploads/<uuid>` accepted, sessionStatus enum, money reject, 9-role parity); `node --check` clean; guarded boot mounts `/v2/contracts` + `/v2/client/contracts`. No schema/PDF/upload change. Commit `ef95b73`.

### Stage 4 — Leaf domains backend (questions + sales-stages + reviews) — ✅ 2026-06-07
**Backend-only (FE deferred).** Migrated three small SHARED-gated leaf domains into v2 modules mounted under `/v2` (strangler): **questions** ← `routes/questions/questions.js` (9 ep) → `/v2/questions`; **sales-stages** ← `routes/shared/sales-stages.js` (2 ep) → `/v2/sales-stages`; **reviews** ← `routes/shared/reviews.js` (3 ep, Google OAuth) → `/v2/reviews`. Each route→controller→usecase→repository(+validation); heavy/side-effecting legacy logic adapts via lazy import. **NOTE — `routes/clients/clients.js` (the client-facing aggregator) was NOT migrated here:** it wires in the frozen image-session (#7) + contracts (#6) sub-routers and the already-migrated chat/calendar, so it is **deferred to the public-client cutover** after those frozen modules land. **SECURITY — closed real IDOR holes the migration surfaced (legacy had ZERO object scope on these lead-coupled routes):** *questions* — QuestionType/BaseQuestion/ObjectionCategory are global config (code is the gate); SessionQuestion/Answer/VersaModel/VersaStep are LEAD-scoped → every lead-scoped route resolves the parent lead (from path `clientLeadId`, or indirectly `sessionQuestionId→lead` / `stepId→VersaModel→lead`) and runs the leads keystone checker BEFORE any read/write; **READS use `checkIfUserCanAccessLead`** (incl. the claimable unassigned-NEW pool), **WRITES (submit-answer, bulk, custom-question, versa create, versa-step edit) use the stricter `checkIfUserCanMutateLead`** (owned-only — a non-owner is 403'd even on a claimable NEW lead); bulk answers scope-check EVERY item; `getQuestionTypes` intentionally stays on access-scope (a view entrypoint whose `ensureSessionQuestions` seed is an idempotent side-effect, preserving the claim-decision UI); bodies `.strict()`, path ids authoritative, acting `userId` from `req.auth`. *sales-stages* — SalesStage is lead-scoped; GET stays access-scope; **set-stage is a workflow write (`POST /:clientLeadId/actions/set-stage`, renamed from `POST /:clientLeadId`) gated by `checkIfUserCanMutateLead`**; stage value enum-validated against `SalesStageType`. *reviews* — OAuth callback NO LONGER returns the raw tokens object (legacy leaked it) → returns only `{connected:true}`; no access/refresh token or client secret returned or logged. **Ported quirks flagged (not changed):** reviews no-state OAuth (CSRF), studio-wide shared `oauth2Client`, stale localhost redirect + empty creds (flow non-functional as frozen) — preserved 1:1. **Security review (shared-security): verdict SAFE** (the one HIGH — writes gated by read-scope — was reworked to mutate-scope before commit). **Authorization:** QUESTION/SALES_STAGE/REVIEW permissions granted via `SHARED_AUTHED` = all 9 authed roles (matches legacy SHARED gate; not widened); reads vs writes split. **Contract:** envelope + questionsMessages/salesStagesMessages/reviewsMessages (language-neutral codes); `.strict()` bodies; Zod-coerced params; reads keep natural (non-list) shapes. **RENAMES (FE repoint):** sales-stages `POST /:clientLeadId` → `POST /:clientLeadId/actions/set-stage`; reviews callback returns `{connected:true}` instead of the token object. **Verified:** root `npm test` green — **398 tests / 27 files** (+38 incl. write-deny-on-claimable-NEW vs owner-allow per write path, bulk per-item mutate-scope, read-still-allows-claimable-NEW, 9-role parity, reviews no-token-leak, stage enum reject); `node --check` clean; guarded boot mounts the three. No schema/PDF/upload change. Commit `e3da3a8`.

### Stage 4 — Dashboard domain backend (9 role-scoped aggregations) — ✅ 2026-06-07
**Backend-only (FE deferred).** Migrated the legacy `routes/shared/dashboard.js` (9 read-only GET aggregations, SHARED gate) into `server/src/modules/dashboard/` mounted `/v2/dashboard` (strangler). Heavy aggregation math reused 1:1 from legacy `dashboardServices.js` via lazy adapters (logic-frozen); the SCOPING decision lives in the v2 usecase. Endpoints: key-metrics, leads-status, monthly-performance, emirates-analytics, leads-monthly-overview, week-performance, latest-leads, recent-activities, designer-metrics. **THE SECURITY FIX — closed a cross-user over-exposure IDOR class (dashboard equivalent of the notifications fix):** legacy let a SCOPED role (sales/designer/executor) read ANOTHER user's metrics via client `?staffId` (and `?userId` on recent-activities), and several endpoints returned GLOBAL totals to a non-admin who omitted the param. v2 derives the scoping identity from `req.auth` (token) and forces the non-admin's OWN id (`Number(authUser.id)`); only the admin-tier union (ADMIN/SUPER_ADMIN/isSuperSales, read from the token) may honor a client `staffId`/see global. **No trusted-id passthrough:** `#buildSearchParams` builds a fresh object and explicitly picks only `{staffId(auth-derived),startDate,endDate,profile}` — never spreads `...req.query`, so a raw client staffId/userId/role can't reach the legacy scoping arg (security review verified the fix is REAL, not illusory). Role-branching (incl. the ADMIN commission-recompute side-effect in leads-status) uses the token role, never `?role=`. **Defense-in-depth (the review's one LOW, applied):** non-admin self-scope coerced to a number + the recent-activities repository THROWS 403 (`ACCESS_DENIED`) if a non-admin scope fails to resolve to a finite id — prevents a future non-numeric token id from silently collapsing the `where` to `{}` and re-opening the IDOR. **No PII/secret over-exposure:** metric endpoints return aggregate numbers; latest-leads selects only `{id,status,createdAt,client:{id,name}}` (legacy-equal); recent-activities returns Notification rows (no sensitive fields). **Security review (shared-security): verdict SAFE — no Critical/High/Medium.** **EXCLUDED (flagged, left on legacy):** `GET /staff/dashboard/latest-calls` (STAFF-gated call-reminder data — belongs to leads/staff-residual). **Contract deltas (recorded in `03-backend-plan.md` §12):** the 5 non-role endpoints now self-scope non-admins (legacy returned global to a non-admin who omitted `?staffId`; week-performance.newLeads stays a global aggregate — frozen math); recent-activities admin-tier honors only `?staffId` now (legacy also honored `?userId`); latest-leads unchanged (global newest-5 NEW leads to all authed roles — preserved). **Authorization:** `DASHBOARD_PERMISSIONS { VIEW: "dashboard.view" }` granted via `SHARED_AUTHED` = all 9 authed roles (matches legacy SHARED gate; not widened) — the data boundary is per-endpoint auth-scoping, not the code. Envelope + 9 language-neutral `dashboardMessages` codes; aggregation payloads keep legacy object/array shapes (no `{items,total,page,pageSize}` forced onto metrics); query Zod-coerced (staffId positive int); no JSON.parse of client input. **Verified:** root `npm test` green — **360 tests / 23 files** (+90 incl. per-endpoint self-scope allow-vs-deny for every scoped role, `?staffId`/`?userId` cannot view others, `?role=` cannot escalate, admin-tier preserved, non-numeric-id 403 guard, 9-role parity); `node --check` clean; guarded boot mounts `/v2/dashboard`. No schema/PDF/upload change. Commit `bf5845b`.

### Stage 4 — Notifications + Utilities domain backend — ✅ 2026-06-07
**Backend-only (FE deferred).** Migrated the notifications subdomain + the utility/lookup helpers into two v2 modules mounted under `/v2` (strangler). **`server/src/modules/notifications/`** ← legacy `GET /utility/notification/unread` + `POST /utility/notification/users/:userId` (both **UNAUTHENTICATED** in legacy) and `GET /shared/utilities/notifications` (SHARED gate) → `/v2/notifications` (+ `POST /v2/notifications/actions/mark-read`). **`server/src/modules/utilities/`** ← legacy `/shared/utilities/*` (SHARED gate): fixed-data, user-logs, users/role/:userId, users/admins, roles, images, model read (`/`), model ids (`/ids`) + `GET /utility/search` → `/v2/utilities/*`. **OUT OF SCOPE (left on legacy):** `POST /utility/upload-chunk` (🔒 frozen chunk mechanism) + `POST /utility/upload` — owned by the already-migrated v2 upload module (not duplicated). **THE IDOR FIX (migration's point):** the two notification endpoints were unauthenticated and scoped by a CLIENT-SUPPLIED userId (query on read, `:userId` path on mark-read) → anyone could read/mark ANY user's notifications. v2 gates both with `requireAuth` + a NOTIFICATION code and derives the subject ONLY from `req.auth.id`; the repository hard-binds `where.userId = authUser.id` on every read AND the mark-read `updateMany`; `filters` JSON parsed safely (try/catch, date-range only); mark-read body `.strict() {}`. The clean reimplementation does NOT call legacy `getNotifications(searchParams)` (no trusted-userId passthrough — verified by the security review as a REAL, not illusory, fix); formerly-unauth endpoints now 401. **Security review (shared-security) returned DO-NOT-COMMIT; reworked then re-reviewed:** (1) **HIGH user-logs IDOR (ported)** — `GET/POST /v2/utilities/user-logs` trusted a client `userId` (forge/read another employee's `UserLog`; `@@unique([userId])` made forged submits collide) → now self-scoped to `req.auth.id`, `userId` removed from the Zod schemas (`.strict()` rejects it). **BEHAVIOR CHANGE: admin-on-behalf-of intentionally dropped here** — admin log access belongs to the users module (`USER.VIEW_LOGS`). (2) **MEDIUM generic model-read passthrough (ported)** — legacy `getModelIds` spread client `JSON where/select/include` into `prisma[model].findMany` (relation traversal + arbitrary column select on allow-listed models) → v2 drops all client `where/select/include`, reads run through a repository with a FIXED per-model pick-list projection (id + label), `modelQuery` `.strict()` (model only). (3) **LOW allow-list correctness** — `UTILITY_MODEL_ALLOWLIST` had non-existent Prisma delegates (image/pattern/color/imageSession) → rewritten to REAL delegates only (designImage, colorPattern, space, material, style, fixedData) each with a fixed projection (`UTILITY_MODEL_PROJECTIONS`); `clientImageSession` EXCLUDED (client-scoped, not a safe reference pick-list). **FE-REPOINT CONTRACT NOTE (for when the FE migrates onto `/v2/utilities`):** model pick-list names change (`image→designImage`, `pattern`/`color→colorPattern`, `imageSession` removed); relation-titled models (colorPattern/space/material/style) return `title` as a relation → read `title[].text`; `designImage`→`{id,imageUrl}`, `fixedData`→`{id,title}`; notification lists normalized `{data,totalPages,total}`→`{items,total,page,pageSize}`; user-logs no longer accept a `userId` param. **Authorization:** `NOTIFICATION_PERMISSIONS` + `UTILITY_PERMISSIONS` granted via `SHARED_AUTHED` = all 9 authed roles (matches legacy SHARED/any-authed; not widened); notifications moved unauth→authed (the IDOR close; no legitimate public caller — per-user data). **Overlap flags recorded (not duplicated):** user-logs/users-admins/roles overlap the users module but legacy served them to all authed roles (narrowing would change behavior) — kept broad here, flagged for a future consolidation decision; the model/image reads lean toward the future image-sessions module (#7) and can be reclaimed/repointed there. **Verified:** root `npm test` green — **270 tests / 21 files** (+40 incl. notification self-scope allow-vs-deny, unauth→401, user-log self-scope, model projection not-honoring client select/include, non-whitelisted model rejected, 9-role parity); `node --check` clean; guarded boot mounts `/v2/notifications` + `/v2/utilities`. No schema/PDF/upload change. Commit `6cac14e`.

### Stage 4 — Calendar domain backend (availability + google-oauth + public client-booking) — ✅ 2026-06-07
**Backend-only (FE deferred).** Migrated the legacy calendar routers into `server/src/modules/calendar/` mounted under `/v2` (strangler — legacy stays live). Three sub-modules: **availability/** (legacy `routes/calendar/calendar.js`, 8 ep, SHARED gate) mounted at **BOTH** `/v2/calendar` and `/v2/calendar-management` to mirror the legacy double-mount (`routes/shared/index.js`); inline `availableDay`/`availableSlot` prisma deletes moved into `availability.repository.js` (identical semantics). **google/** (legacy `routes/calendar/google.js`, 5 ep, OAuth sub-router at `/google`) — Google access/refresh tokens + client secret are NEVER selected or returned (repo selects only connection metadata: connected flag, calendarId, token-expiry); no token/secret logging; callback derives user from the OAuth `state` (preserved 1:1). **client/** (legacy `routes/calendar/client-calendar.js`, 6 ep) mounted **PUBLIC** at `/v2/client/calendar` (no auth, no permission code) — token-based booking (`MeetingReminder.token` via `verifyAndExtractCalendarToken`), same rule as the booking funnel / `/files/client/*`; `reminderId`/`clientLeadId` come from the verified TOKEN, never the body (hardening, tested); `/book` body `.strict()`. **Dead code NOT migrated:** `routes/calendar/old-call.js` (mounted nowhere, broken imports to non-existent services — no observable behavior); `new-calendar.js` is a service file (under `routes/`) invoked via lazy adapters, not a router. **Authorization:** `CALENDAR_PERMISSIONS` (view/manage/google.view/google.manage) granted via `CALENDAR_AUTHED` on `SHARED_AUTHED` = all 9 authed roles, reproducing the legacy SHARED gate EXACTLY (verified vs `verifyTokenAndHandleAuthorization`; not widened). No per-owner object scope on availability in legacy → the permission code IS the gate (documented); Google actions self-scoped to `req.auth.id`. Reads/writes split. **Contract:** envelope + `calendarMessagesCodes` (language-neutral, no prose), `.strict()` bodies, Zod coercion + finite/bounded numeric validation; paths preserved 1:1 (no workflow-action renames — reads/availability writes, not status transitions). **Security review (shared-security): verdict SAFE — ZERO introduced vulnerabilities;** all Medium/Low/INFO findings are verbatim-ported legacy quirks, preserved per the behavior-freeze and documented in module headers. **HARDENING BACKLOG (to raise with the user — NOT changed here, these are access-control behavior decisions present identically in the still-live legacy routes):** (1) availability `DELETE /days/:id` + `/slots/:id` have no ownership/booked-slot guard (any `calendar.manage` holder — i.e. every authed role — can delete any admin's availability; likely intended shared-studio behavior, **confirm**); (2) OAuth `state` is an unsigned user id → sign/nonce it and prefer `req.auth.id` on callback; (3) `/book` trusts `selectedSlot.id` without a slot-belongs-to-admin check. **Verified:** root `npm test` green — **230 tests / 19 files** (+53 from calendar incl. 9-role parity allow-vs-deny, read/write split denial, month-view role-filter parity, Google no-token-leak + already-connected guard, public-booking token-cannot-be-overridden, `.strict()` rejection, numeric bounds); `node --check` clean; guarded boot mounts `/v2/calendar`, `/v2/calendar-management`, `/v2/client/calendar` (public 200, authed 401). No schema/PDF/upload change. Commit `174e8e1`.

### Stage 4 — Accounting domain backend (payment + expense + note + rent + salary + report) — ✅ 2026-06-07
**Backend-only (FE deferred).** Extracted the legacy `routes/accountant/accountant.js` (20 endpoints, `services/main/accountant/accountantServices.js`) into a layered module tree `server/src/modules/accounting/{payment,expense,note,rent,salary,report}/` (route→controller→usecase→repository+validation+dto where applicable), mounted `/v2/accounting` (strangler). Heavy/side-effecting financial logic adapts to the existing legacy accountant services via lazy adapters → behavior 1:1 (no logic duplication). **Authorization parity (VERIFIED against `verifyTokenAndHandleAuthorization`):** the legacy `ACCOUNTANT` gate admits ONLY base-role ACCOUNTANT — it falls through to the `decoded.role !== role` branch; the `isAdmin` early-return fires only when the gate param is `"ADMIN"`, so ADMIN/SUPER_ADMIN/isSuperSales do NOT pass today. To preserve this 1:1, `ACCOUNTING_PERMISSIONS` are granted to **ACCOUNTANT only** in `role-permissions.js` (no ADMIN/SUPER_ADMIN/isSuperSales widening — that would be a behavior change). These are GLOBAL financial records (accountant operates on all), so there is no per-owner object scope in legacy and the permission **code IS the gate**; every route is `requirePermissions`-gated with reads vs writes split into distinct codes. Contract: envelope + `accountingMessagesCodes`, `{items,total,page,pageSize}` lists, per-record `capabilities.*`. **Security review (shared-security) — verdict SAFE, no CRITICAL/HIGH; hardening applied as defense-in-depth on the money module:** (1) money validation at the edge — `amount`/`baseSalary`/`baseWorkHours`/`netSalary`/`totalHoursWorked` are `z.coerce.number().refine(finite && > 0)`, tax/overtime/bonuses/deductions `>= 0` (rejects negative/zero/NaN/non-finite, tested); (2) `newPaymentLevel` enum-validated against `PaymentLevel` (legacy took any string); (3) workflow actions — `POST /payments/:id/actions/{pay,mark-overdue,change-status}` (was `POST .../pay`, `.../overdue`, `PUT .../status`), each existence-guarded via `requireSpecialChecker` before any money mutation; (4) mutating body schemas `.passthrough()`→`.strict()` (mass-assignment defense on money/salary records — currently neutralized by downstream destructuring but hardened at the edge); (5) dropped the unused **client-trusted `oldPaymentLevel`** from change-status (the legacy `changePaymentLevel` ignored the 3rd arg); (6) safe-parse the `filters` query JSON (no 500 on malformed/absent input); (7) language-neutral message CODES (no legacy `"Paid succussfully"` prose); `userId` always from `req.auth`, never client body; user/salary selects narrowed (no password-hash leak). **Recorded ported-legacy quirks (not changed):** the salary `getUsersWithSalaries` `filters` parse lives in the unmigrated legacy service (left as-is); note `idKey` client-controlled Prisma field name (Prisma-validated, can't cross tables — 500 at worst). **Verified:** root `npm test` green — **177 tests / 17 files** (+25 from accounting incl. money-validation reject cases, ACCOUNTANT-only vs ADMIN-403 permission parity, workflow-action existence guards); `node --check` clean; guarded boot mounts `/v2/accounting`. No schema/PDF/upload change. Commit `d2bce49`.

### Stage 4 — Projects domain backend (project + task + update + delivery) — ✅ 2026-06-07
**Backend-only.** Extracted the four coupled legacy shared surfaces (`routes/shared/{projects,tasks,updates,delivery}.js` — 30 endpoints) into a layered module tree `server/src/modules/projects/{project,task,update,delivery}/` (six files each) sharing ONE project-scope checker (`projects/shared/project-scope.js`); mounted `/v2/{projects,tasks,updates,delivery}` (strangler). **The IDOR keystone for designers/executors:** access boundary = `Project↔ClientLead` via `Assignment.userId` (verified against frozen schema + legacy services). `buildAuthUserProjectWhere` narrows non-full-scope roles to `assignments.some.userId=self` (merged under `AND` so a caller filter can't widen scope); `checkIfUserCanAccessProject` (read) / `checkIfUserCanMutateProject` (write) throw 403 with no existence leak; **ACCOUNTANT is full-READ only** (cannot mutate); task/update/delivery resolve their parent project/lead and re-run the same checker; every object-scoped route carries `requirePermissions` + the checker. Status changes → `POST /:id/actions/<kebab>` (assign-designer, change-status, authorize, authorize-shared, archive, mark-done, link-meeting); plain field edits stay PUT (renames documented for FE). Workflow-guard hardened: project `oldStatus`/`isAdmin` derived server-side, client value dropped. Contract: envelope + `projectsMessagesCodes`, `{items,total,page,pageSize}` lists, per-record `capabilities.*`; `PROJECT/TASK/UPDATE/DELIVERY` permissions (PROJECT_AUTHED to all authed = legacy SHARED gate; `*.MANAGE` to ADMIN-tier). Heavy aggregators/side-effects delegate to existing services via lazy adapters → behavior 1:1. **Security review (shared-security) — 2 HIGH + 1 MED fixed (all ported-legacy holes the migration closed):** (1) **broad-delete IDOR** — `task DELETE /:id` mapped to a generic `deleteAModel` over any `body.model` (delete arbitrary <5-min rows + client-driven cascade) → restricted to `z.literal("Task")`, always project-scoped, no client cascade forwarded; (2) **PII enumeration** — `GET /user-profile/:userId` was unscoped and returned full `Client` rows (name/phone/email) for any user's projects → self-or-admin scope checker; (3) **mass-assignment** — `PUT project/:id` + `PUT task/:taskId` `.passthrough()` → `.strict()` whitelists (id/userId/clientLeadId/relations blocked); + query coercion on `GET /`. **Reconciliation note recorded:** the FE project/task edit forms still post to the LEGACY endpoints and spread the full object — when repointed to v2 they must send only whitelisted fields (FE-migration TODO). **Verified:** root `npm test` green — **152 tests / 15 files** (+38 from this domain incl. designer-IDOR allow-vs-deny, parent-scope resolution, workflow-guard, broad-delete + user-profile + strict-schema); `node --check` clean; guarded boot mounts all four. No schema/PDF/upload change.

### Stage 4 — Users module backend + shared validation fix — ✅ 2026-06-07
**Backend-only.** Merged three legacy user surfaces into a layered module `server/src/modules/users/user/` mounted `/v2/users` (strangler): admin user-management (`admin.js` `/users*`, `/all-users` — list, create, update, roles, restricted-countries, auto-assignments, max-leads[-per-day], staff-extra, logs, last-seen, status), the chat/assignment **directory** (`/shared/users` + `/shared/all-related-chat-users` → `/v2/users/directory` + `/related-chat-directory`, which **unblocks the chat module's deferred `/v2/users` TODO**), and self **profile** (`shared/users.js` `/:userId/profile`). Out of scope (left on legacy): `/reports/staff-report*` (frozen pdfkit → future reports module) and `/client-leads/:leadId/telegram/assign-users`. Heavy/side-effecting ops (create/edit + bcrypt, roles, auto-assignments, logs) delegate to existing services via lazy adapters → behavior 1:1. Contract: envelope + `userMessagesCodes`, `{items,total,page,pageSize}` lists, per-record `capabilities.*`. `USER_PERMISSIONS` added; authorization preserves the legacy ADMIN gate at the **code** layer (ADMIN/SUPER_ADMIN base + isSuperSales-extra + sub-role union; verified against `verifyTokenAndHandleAuthorization`), directory+profile codes broad (all authed, = legacy SHARED gate). **Security review (shared-security) — fixed:** serious **profile IDOR** (`GET|PUT /:userId/profile` had NO ownership check, returned the full row incl. the **bcrypt password hash**, and did a **blind-passthrough update** → any authed user could read or escalate any account) → throwing self-or-admin scope checkers + `toSafeProfile` strips `password` on every path + non-admin self-edit whitelisted to `{name,telegramUsername,profilePicture}`; **staff-extra mass-assignment** (`.passthrough()`, reachable by isSuperSales) → `.strict()` whitelist `{isPrimary,isSuperSales}`; **admin profile-edit** raw-body→Prisma (plaintext password, arbitrary fields) → allow-listed + bcrypt-hashed (cost 8, matched to legacy `editStaffUser`); **`isAdminTier`** now consults sub-roles (fixes a subRole-admin profile-scope regression + internal inconsistency); **`findUserProfileById`** includes `subRoles` (legacy admin-profile FE parity). **Plus a shared cross-cutting fix** (flagged by both the leads & users reviews): `validate.middleware.js` was putting raw Zod prose into the envelope `message` (e.g. `"userId: Expected number, received nan"`) — a contract violation + internals leak on every 422 across all modules — now throws `generalMessagesCodes.VALIDATION_ERROR` with the per-field reasons kept in `details`. **Verified:** root `npm test` green — **114 tests / 12 files** (+23 from users incl. profile IDOR allow-vs-deny, password-strip, mass-assignment rejection, subRole-admin scope, subRoles parity); `node --check` clean; guarded boot mounts `/v2/users`. No schema/PDF/upload change.

### Stage 4 — Leads / clientLead CORE backend (the IDOR/security keystone) — ✅ 2026-06-07
**Backend-only (FE deferred).** Extracted the **27** legacy `routes/shared/client-leads.js` endpoints into a layered six-file module `server/src/modules/leads/lead/` (route→controller→usecase→repository+validation+dto), mounted `/v2/leads` alongside the legacy router (strangler). Behavior ported 1:1: list/deals/columns pool+self+country narrowing (`LIST_FULL_ROLES`, `notAllowedCountries`, `selfId/userId` injection), admin-vs-staff detail branches, calls/meetings/price-offers/payments/files/notes sub-resources, payment/complete-register reminders. Heavy side effects (notifications/telegram/stripe/BullMQ/`updateLead`) invoked via the **existing** services through lazy adapters (same pattern as courses) so observable behavior is preserved without duplicating thousands of lines. Contract: envelope + `leadsMessagesCodes`, `{items,total,page,pageSize}` lists, per-record `capabilities.*` (canEdit/canChangeStatus/canConvert/canAssign*/canAdd*/canSendReminder) on lists+detail; `PUT /:id/status` → **`POST /:id/actions/change-status`** (workflow-action convention). `LEAD_PERMISSIONS` added (LEAD_AUTHED to all roles, `lead.assign.other` to ADMIN/SUPER_ADMIN + isSuperSales). **The IDOR keystone:** every object-scoped `/:id` route now carries `requirePermissions` **plus** a throwing object-scope checker — read vs write split: `checkIfUserCanAccessLead` (view scope incl. the claimable NEW/unassigned pool) vs `checkIfUserCanMutateLead` (owned-only); `buildAuthUserLeadWhere` merges ownership under `AND` so a caller-supplied `OR` can't clobber scope, and a null result → 403 with no existence leak; sub-resource checkers (call/meeting reminders, price offers) resolve the parent lead and re-run lead scope. **Security review (shared-security) — 2 HIGH fixed:** (1) `GET /meeting-reminders/:meetingId` was `VIEW`-only (any authed role) and `include:{user:true}` leaked the full assignee `User` row → added an access checker (resolve parent lead → `checkIfUserCanAccessLead`) and narrowed all meeting-reminder user includes to `{id,name}`; (2) `change-status` trusted client-supplied `oldStatus`, letting a scoped owner forge it to bypass the FINALIZED/REJECTED/ARCHIVED/ON_HOLD transition lock → `oldStatus` now derived server-side from the scoped row (`req.scoped.status`), client value dropped. Param-name wiring (the bug class that bit chat/courses) verified correct on every route; no double `validate("params")`. **Recorded ported-legacy quirks (not changed, behavior-preserving):** the `deals` `isAdmin = … || role !== "SUPER_SALES"` `!==` expression (only affects update-visibility branch, no lead scope impact); unparseable `filters` → 500 as in legacy; 11 reserved domain-rule codes declared for the later side-effect-migration phase. **Verified:** root `npm test` green — **85 tests / 10 files** (incl. the updated super-sales-extras assertion); `node --check` clean on all module files; guarded boot mounts `/v2/leads`. No schema/PDF/upload change.

### Stage 4 — Courses / LMS backend (extraction + security review + rework) — ✅ 2026-06-06
**Backend-only (no FE this batch, per user).** Extracted `routes/courses/{admin,staff}Courses.js` + `services/main/courses/*` into two layered six-file surfaces: **`admin-course`** (course/lesson/video/pdf/link/test/question CRUD, lesson-access grants, attempt admin, answer-approval re-scoring, dashboard) → `/v2/courses`, and **`staff-course`** (role-filtered browse, lesson-access gating, homework, test-taking, attempts/progress, dashboard) → `/v2/staff-courses`. Behavior ported 1:1 (same Prisma queries, scoring math, attempt-limit invariants); legacy routers stay mounted (strangler). Permission codes (`COURSE_PERMISSIONS` → ADMIN/SUPER_ADMIN/isSuperSales/admin-subrole, matching legacy `isAdmin`; `STAFF_COURSE_PERMISSIONS` → all authed) + `coursesMessagesCodes`; envelope+CODES, `{items,total,page,pageSize}` lists, per-record `capabilities.*`; lesson-complete `PATCH`→`POST /actions/complete`. **Security review (shared-security) found real exploitable issues — all fixed:** C1/C2 **cross-user IDOR** on submit-answer + end-attempt (now owner-scoped via `checkIfUserCanMutateAttempt` special checker — these were pre-existing legacy holes), H2 question↔test binding check, H1 terminal-state guard (staff can't re-end a finalized attempt; admin re-score preserved via explicit `reScore` flag), M1 atomic attempt-limit (`$transaction` + `FOR UPDATE`, closes TOCTOU), M2 mass-assignment (`.passthrough()`→whitelisted `.strict()` on admin edit bodies), L1 (getTest bypass keys on permission not role), L2 (stop leaking full user rows in attempt lists). **Verified:** root `npm test` green — **85 tests / 10 files** (incl. new owner/403, foreign-question, terminal-state, admin-rescore tests); 119 course routes mount on guarded boot. No schema/PDF/upload change.

### Stage 4 — site-utility module (from-scratch extraction + FE + review + rework) — ✅ 2026-06-06
First **legacy→module** extraction. **BE** (`server/src/modules/site-utility/`): migrated the 6 legacy `/site-utilities` endpoints (`GET|POST /pdf-utility`, `GET|POST|PUT|DELETE /contract-payment-conditions[/:id]`) from `routes/site-utilities/siteUtility.js` + `siteUtilityServices.js` into a clean six-file layered module, mounted at `/v2/site-utilities` (legacy stays live — strangler). Adopted the envelope + CODES (`siteUtilityMessagesCodes`), `{items,total,page,pageSize}` for the conditions list with per-row `capabilities.{canEdit,canDelete,inUse}`, singleton shape for pdf-config, Zod (422+details), preserved legacy invariants (reserved "To Do" reject; in-use delete guard). Added `SITE_UTILITY_PERMISSIONS` (6 codes) to `@dms/shared` granted to **ADMIN+SUPER_ADMIN only** — a **security fix**: legacy routes were authentication-only, so any logged-in role could mutate PDF config + contract payment conditions. **FE** (`ui/src/app/v2/features/siteUtility/`): migrated the `website-utilities` admin UI (PDF-utility field editor + payment-conditions CRUD) into the feature pattern on `apiFetch`/`useRequest`, Arabic RTL, URL-driven tabs, chunked upload via v2 `useUpload`; mounted additively at `(v2-features)/v2/site-utilities`. **Review** found a blocker (FE permission code names didn't match BE → gating silently failed) + a should-fix (FE ignored per-row `capabilities` → Delete would 409 on in-use rows); both **reworked**: FE codes now byte-match the BE `@dms/shared` strings (verified), Delete gated on `capabilities.canDelete` with an "in use" disabled state. **Verified:** root `npm test` green — **54 tests / 7 files**; BE guarded boot mounts `/v2/site-utilities` (401 unauthenticated); FE esbuild parses + bundles clean. PDF *generation* logic untouched (this is PDF *config* CRUD). Orchestrator TODO: permission-gated nav entry; the legacy "Contract utility" tab belongs to the future contract module.

### Stage 4 — Chat module (BE harden + FE feature + review + rework) — ✅ 2026-06-06
**Pilot of the BE∥FE→review→rework loop.** **BE** (`server/src/modules/chat/`): finalized onto the `{success,message(CODE),data,translationKey}` envelope (added `chatMessagesCodes` to `@dms/shared`); normalized the true list endpoints (`GET /rooms`, `/rooms/:roomId/members`, `/rooms/:roomId/files`) to `data:{items,total,page,pageSize}` while **preserving** the cursor `{data,total,totalPages}` shape of message history (infinite scroll intact); moved per-record `capabilities.*` (`canEdit/canDelete/canManageMembers/canManageClient/canSendMessage/canUploadFiles`) into the dto and attached to room list+detail; closed the IDOR gap by adding the throwing `checkIfUserCanAccessRoom` scope checker to **every** `/rooms/:roomId/*` route (was only on `GET /rooms/:roomId`). **FE** (`ui/src/app/v2/features/chat/`, 37 files): migrated the legacy chat UI into the feature pattern preserving behavior/appearance (no UX redesign), all data via `chatService`→`apiFetch` on `/v2/chat/*`, realtime via the existing `SocketProvider`, gated by `usePermission` × per-record `capabilities.*`; added the FE permission mirror `v2/config/permissions.js` + `usePermission` + `useScroll`; single Arabic/RTL with CODE→Arabic toast resolution. **Reconciliation review** found 5 real issues, all reworked & verified: (1) FE message parser read `res.data` not `res.data.data` → messages rendered empty (fixed); (2) FE leave-room sent `"me"`→NaN→422 → now sends `room.selfMember.id`; (3) add-members directory hit non-existent `/v2/users` → now uses legacy `/shared/all-related-chat-users` + `/admin/all-users` via `legacyApiFetch` through `chatService` (TODO: switch to `/v2/users` when that module migrates); (4) BE raw `"Message not found"` → `chatMessagesCodes.MESSAGE_NOT_FOUND`; (5) **confirmed BE bug** — double `validate("params")` on member `DELETE`/`PUT` dropped ids (member remove/update was broken) → fixed with a single combined `roomMemberParams` schema + regression test. **Verified:** root `npm test` green — **37 tests / 5 files**; esbuild parses all 37 FE files + bundles the import graph (exit 0). Socket realtime, schema, PDF, and upload-chunk logic untouched; `/v2/chat` URLs frozen. (Orchestrator TODO for the FE: a permission-gated nav entry to chat + folding the temporary `(v2-features)` route shell into the real dashboard shell when it lands.)

### Stage 3 — Auth + Permissions framework (BE Phase 2) — ✅ 2026-06-06
Built the **code-defined** permission system the frozen schema requires (no permission tables exist — only `User.role`/`isSuperSales`/`subRoles`). In `@dms/shared`: filled `constants/access/permissions.constants.js` with `dot.case` codes grouped per **existing** module (`AUTH_PERMISSIONS`, `CHAT_PERMISSIONS`, `TELEGRAM_PERMISSIONS`, `UPLOAD_PERMISSIONS`) + nested `PERMISSIONS.<DOMAIN>.<ACTION>`, `splitPermissionCode`, `ALL_PERMISSIONS`, and a documented append-convention for future modules (booking-lead intentionally has NO codes — it's the public funnel); added `roles.constants.js` (`USER_ROLES` mirrored from the frozen `UserRole` enum) and `role-permissions.js` (`ROLE_PERMISSIONS` mapping every role, `SUPER_SALES_EXTRA_PERMISSIONS`). The map **preserves current behavior**: telegram codes → ADMIN/SUPER_ADMIN only (was `requireRole(["ADMIN"])`); the chat + authed-upload + `auth.me/logout` surface → every role (those routes gate on auth alone today); effective = base role ∪ each sub-role's codes ∪ isSuperSales extras. Added pure helpers in `helpers.js`: `getEffectivePermissions(user)→{permissions[],permissionsByModule{}}` (tolerant of Prisma `{subRole}` rows and string[]), `getPermissionsForRole`, `hasPermission/hasAll/hasAny`, and the `computeCapabilities(rules, ctx)` per-record capabilities pattern. Added `messages-codes/auth/auth.js` (`authMessagesCodes` incl. `FORBIDDEN`/`ACCESS_DENIED`) + `LEGACY_AUTH_COOKIE_NAME="token"` in `auth.js`. **Backend:** `shared/middlewares/auth.middleware.js` now: `requireAuth` verifies the unified `access_token` (transitional read-shim still accepts the legacy `"token"` cookie via `JwtService.verifyLegacyAccess`/`SECRET_KEY`) and attaches `req.auth` with flattened effective permissions computed from the token (role+subRoles+isSuperSales embedded, no DB hit); new `requirePermissions(required, anyOf)` (all-of / any-of → 403 `FORBIDDEN`); new `requireSpecialChecker(fn)` (object-scope gate — forwards the checker's throw, stashes the row on `req.scoped`); `requireRole` kept but `@deprecated`. **JWT unified** (`infra/security/jwt.js`): always sign/verify with the access/refresh secrets, removed the `currentMainTokenName`/`LEGACY` bridge; cookie names sourced from `@dms/shared`. **`auth/me`** (`auth.dto.js` `toMe` + controller): returns user display fields (`role`, `activeRole`, `subRoles`, `isSuperSales`, `isPrimary`, `profilePicture`) + `permissions[]` + `permissionsByModule{}`; login now embeds `subRoles` in the token and issues ONLY the new pair (no legacy `token` cookie); removed stray `console.log`s; replaced raw-string `AppError`s with `authMessagesCodes`; `response.js` helpers now emit `translationKey`. **Renamed `auth.repo.js`→`auth.repository.js`** and repointed the usecase. **Re-gated the 5 existing v2 modules onto codes** (same roles keep access): telegram `requireRole(["ADMIN"])`→`requirePermissions([TELEGRAM.MANAGE])`; authed upload `/single`+`/chunks`→`requirePermissions([UPLOAD.FILE_UPLOAD])` (client `/files/client/*` left **public**); chat every route → its read/write `CHAT.*` code, with `GET /rooms/:roomId` carrying the **concrete reference scope-checker** (`checkIfUserCanAccessRoom` controller→usecase via the `ChatMember` FK, throws 403 `ACCESS_DENIED`) and the **concrete capabilities example** (`canEdit/canDelete/canManageMembers`) attached to the detail response; `auth/me`+`logout`→`AUTH.ME`/`AUTH.LOGOUT`; booking-lead left ungated (public). **Verified:** 25 vitest tests green (15 `getEffectivePermissions`/role-map/capabilities + 9 `requirePermissions`/`requireSpecialChecker` allow-vs-deny + 1 smoke) — proves ADMIN passes & STAFF is 403'd on `telegram.manage`, STAFF passes shared codes, sub-role union elevates, scope-checker throw is forwarded; `node --check` OK on all 22 touched files; `@dms/shared` barrel exposes the full surface with no missing exports; guarded boot from `server/` (`RUN_WORKERS=false RUN_CRON=false PORT=5099`) reached `Server running on port 5099` with no import/wiring error (only the pre-existing `User.allowEmailing` DB drift + telegram skip, both KNOWN/unrelated). **Did NOT** touch the schema, PDF/upload-chunk/worker logic, or any business logic; the legacy `"token"` cookie is still ACCEPTED (read-shim) but no longer ISSUED — full removal is deferred to Phase 12 cutover. NOTE: the host disk is at 100% (0 bytes free) — tests/boot still ran, but `git mv` of `auth.repo.js` could not write the index lock, so the rename is applied in the working tree (file moved on disk; git index reconciles on next commit) — flagged, not worked around.

### Stage 2b — Redis consolidation + server-owned BullMQ workers & cron (BE Phase 2, runtime wiring) — ✅ 2026-06-06
Centralized all **ioredis** connection config behind one factory `src/infra/redis/ioredis.connection.js` (`getIoredisOptions()` / `createIoredisClient()`, sourced from `config/env.js`); repointed `src/infra/redis/bullmq.connection.js` at it and replaced the two ad-hoc `new Redis({...process.env})` instantiations in the live socket pub/sub (`services/redis/socketPublisher.js`, `socketSubscriber.js`) with `createIoredisClient()`, and marked the dead legacy `services/redis/redis.js` + `bullmqConnection.js` as deprecated shims that delegate to the factory. **Both Redis libs intentionally remain**: `redis` (node-redis) backs the cache layer (`redis.client.js`/`cache.service.js`); `ioredis` backs BullMQ (which can't use node-redis) and the Socket.IO pub/sub — they cannot collapse, but now share one config. **Workers** now start from the server bootstrap: new `src/infra/workers/start-workers.js` exposes a gated, idempotent `startWorkers()`/`stopWorkers()` that dynamically imports the worker barrel (so OFF opens no connections) and `src/server.js` calls it after Redis + the single GramJS connect, behind the new `env.RUN_WORKERS` flag (default ON); **fixed the broken `pdf.worker.js` import** (`services/main/clientServices.js` → `services/main/client/clientServices.js`). **Cron** folded into `src/infra/cron/{index.js,reminders.cron.js,project-delivery.cron.js,telegram.cron.js}` with a gated `startCron()`/`stopCron()` called from `server.js` behind `env.RUN_CRON` (default ON); the standalone `reminderScheduler.js`, `projectDeliveryTimeReminder.js`, `tele-cron.js`, and `start-telegram-system.js` were converted to thin **deprecated shims** that delegate to the canonical bootstrap paths (kept runnable for ops; `tele.js` left as the one-off dev tool). While folding, corrected two latent legacy bugs so the runnable path actually works: the reminder scheduler's wrong email-template path (`services/main/emailTemplates.js` → `services/main/email/emailTemplates.js`) and the missing `prisma` import in the project-delivery scheduler. Added `RUN_WORKERS`/`RUN_CRON` to `config/env.js` (default ON; set `"false"` to designate single-instance ownership). **Did NOT change** PDF generation logic, the upload chunk mechanism, or any sync↔async flow — the image-session pdf enqueue at `client-image-session.js:161` stays commented (the inline sync path remains the live one); the fixed pdf worker is merely runnable/correctly-wired, and nothing enqueues to `pdf-approval-queue`. **Verified:** `node --check` on all 19 touched/new files (all OK); `npm test` green (1 passed); import-smoke of the new redis/workers/cron modules all resolve; guarded boot from `server/` with `RUN_WORKERS=false RUN_CRON=false PORT=4137` booted cleanly (`Redis client connected`, `Socket subscriber listening`, both gates logged disabled, `Server running`), and with `RUN_WORKERS=true RUN_CRON=false PORT=4138` logged `Started 6 BullMQ workers` (incl. the fixed pdf worker) with no import/wiring crash — both runs only hit the pre-existing `User.allowEmailing` DB drift inside telegram connect (swallowed, not our bug). NOTE: a bare `node server/index.js` from the repo ROOT crashes earlier in `config/cors.js` because `dotenv` doesn't find `server/.env` from that cwd (origin env vars undefined) — pre-existing env/cwd issue, unrelated to this stage; boot from `server/` as above.

### Stage 2a — Relocate `server/v2/` → `server/src/` + de-dup infra (BE Phase 1, move only) — ✅ 2026-06-06
Relocated the entire `server/v2/` tree to `server/src/` via `git mv` (history preserved); because `v2` and `src` sit at the same depth under `server/`, every relative import pointing *outside* the tree (`../routes/*`, `../../services/*`, `../../prisma/prisma.js`) keeps resolving unchanged, and the new tree has **zero** internal `v2/`-named specifiers. Repointed the legacy files that named `v2/` in **import paths only** (`v2/`→`src/`): `server/index.js`, `server/tele-cron.js`, `server/start-telegram-system.js`, `server/services/socket.js`, `server/services/telegram/connectToTelegram.js`, the six `server/services/queues/*` shims and six `server/services/workers/*` shims; updated the `server/package.json` `dev:v2`→`dev:src` script. **De-duplicated infra**, repointing every importer first then deleting the dead copy: kept `infra/mail/mail.js` (removed `infra/mailer.js`), kept `infra/socket/index.js` (removed `infra/socket.js`; also repointed legacy `services/socket.js`, `chat.usecase.js`, `notification.service.js`, and the previously-dangling `infra/socket/handlers/call.handler.js`), removed the dead `infra/telegram.js` stub (single GramJS owner remains `modules/telegram/manager` + `connect.js`, imported by nobody else), and consolidated Prisma to one canonical in-server module `infra/prisma/prisma.js` (→ `server/prisma/prisma.js` → `@dms/db`), removing the duplicate `infra/prisma.js` and repointing its four importers; the abandoned `infra/prisma/*.prisma` WIP files were left untouched. Resolved the chat dual-repo by deleting the empty dead `chat.repo.js` and keeping the live 824-line `chat.repository.js` (only importers were `chat.socket.js`/`chat.usecase.js`, already on `.repository.js`). **The permanent `/v2` API URL mount (`app.use("/v2", v2Routes)`) and all route/URL strings were left exactly as-is** — only filesystem import specifiers were rewritten. No business logic, routes, validation, schema, chunk/PDF/worker behavior changed. **Verified:** `npm test` green; `node --check` on all 96 moved `src/*.js` + the repointed legacy files (all OK); repo-wide grep shows **zero** remaining `v2/` import specifiers and confirms the `/v2` URL string intact; import smokes for the prisma chain, deduped mail/socket modules, `notification.service.js`, legacy `services/socket.js`, and a full `import('./src/app.js')` all resolve; guarded boot of `server/index.js` reached runtime (Redis connected, Telegram client connected, Prisma issued a live query) and only stopped on a pre-existing DB column drift (`User.allowEmailing` missing) and `EADDRINUSE :4000` from an already-running dev instance — i.e. no import/wiring crash from the move.

### Stage 1 — Foundation (BE Phase 0) — ✅ 2026-06-06
- Established the npm-workspaces monorepo: root `package.json` (`dms-monorepo`; workspaces `packages/*`, `server`, `ui`; node>=24; `"type":"module"`) + root **vitest** runner with a passing smoke test (`tests/smoke.test.js`, `vitest.config.js`).
- Created **`packages/db` (`@dms/db`)**: singleton Prisma client over the **FROZEN** schema relocated byte-identically to `packages/db/prisma/schema.prisma` (sha256 `7e01cbab…`, 2478 lines; verified identical to `server/prisma/schema.prisma`). Kept default `@prisma/client` output + `new PrismaClient()` (no adapter / no custom output). Migrations copied alongside.
- Created **`packages/shared` (`@dms/shared`)** as a SEED: `auth.js` (cookie names), `helpers.js`, `messages-names.js`, `messages-codes/core/general.js`, and an **empty `constants/access/permissions.constants.js` skeleton** (filled in Stage 3).
- **Non-breaking shims:** `server/prisma/prisma.js`, `server/v2/infra/prisma.js`, and `server/v2/infra/prisma/prisma.js` (which had been instantiating a *second* client) all re-export the single `@dms/db` instance. All four import paths resolve to one client.
- Removed `server`'s destructive `postinstall` (auto `migrate deploy` + `node initData.js`) → explicit `db:deploy`/`db:seed` scripts.
- **Verified:** `npm install` (0), `prisma generate` via local 6.x (0), `@dms/db`/`@dms/shared` import smoke (0), shim chain = one instance, `node --check` key files, `npm test` (1 passed).

**Environment caveats to remember:**
- Dev DB has a **pre-existing failed migration** `20250118223321_price_offer_url` (P3009) — unrelated to us, schema frozen; do not "fix" it. Will matter if/when running migrations.
- **Use the local Prisma 6.x** (`npm run db:generate` from root). A bare `npx prisma …` pulls Prisma 7 and errors on the frozen schema.
- Windows + npm 10: nested `-w @dms/db` lifecycle scripts don't get the hoisted `node_modules/.bin` on PATH; root-level scripts do (that's why db scripts live at root).
- `server/prisma/schema.prisma` left in place (canonical generate source is now `packages/db`); deduping the server copy is a Stage 2/Phase-1 follow-up.


