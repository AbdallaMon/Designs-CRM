# Browser E2E Remediation Plan

1. Add regression tests for the sensitive lead relation and profile hierarchy.
2. Narrow lead-detail user selects and enforce `ADMIN > SUPER_ADMIN` list/object scope.
3. Correct shared designer navigation and update parity tests.
4. Stabilize public contract rendering and upload-progress hydration.
5. Fix public registration language and visible/accessible email validation in `eng-ahmed`.
6. Refresh lead counters after assignment and correct headings/dialog copy.
7. Update the reproduced Next/MUI callsites to their supported APIs.
8. Run focused and broad automated verification, then repeat the critical browser flows.
9. Record final state and remaining deployment-only steps in `PROJECT_STATE.md`.
10. Add a capability-protected registration-status read, restore the completed success state after
    refresh, and localize created/uploaded progress and toast copy in `eng-ahmed`.
