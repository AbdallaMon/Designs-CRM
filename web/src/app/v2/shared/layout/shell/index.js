// Barrel for the new sidebar app-shell (frame swap from the rejected workspace-rail shell).

export {
  AppSidebar,
  SIDEBAR_WIDTH_EXPANDED,
  SIDEBAR_WIDTH_COLLAPSED,
} from "./AppSidebar";
export { AppHeader } from "./AppHeader";
export { AppSidebarShell, default } from "./AppSidebarShell";
