// Compatibility names for existing frontend imports. Values come directly from the
// canonical workspace package; this file no longer mirrors any permission string.
export {
  ADMIN_RESIDUAL_PERMISSIONS as ADMIN_RESIDUAL_CODES,
  LEAD_PERMISSIONS as LEAD_CODES,
  PROJECT_PERMISSIONS as PROJECT_CODES,
  TELEGRAM_PERMISSIONS as TELEGRAM_CODES,
  USER_PERMISSIONS as USER_CODES,
} from "@dms/shared";
