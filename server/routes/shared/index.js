/**
 * Shared Routes
 *
 * This module handles all shared routes accessible by authenticated users.
 * Includes client leads, projects, tasks, dashboard, and more.
 */

import sharedRoutes from "./shared.js";

export default sharedRoutes;
