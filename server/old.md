-- inside the chat for client lead if client is added allow admin to send email of latest messages

-- read at and delivered at timestamps for messages
-- allow user to leave group if not added by admin
-- check scroll if room list is long
-- if chat setting change emit socket event to update settings in real time

-- send by email and if we have whatsapp optional send via whatsapp
