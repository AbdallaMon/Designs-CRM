-- on new member addition emit notification:room_created event to each new member

-- group chat settings like allow calls, allow file sharing, allow chat ,change group name/photo .

-- allow user to have settings like ( profile picture , allow notification , allow emailing)
-- inside the chat for client lead if client is added allow admin to send email of latest messages
-- if we have whatsapp optional send via whatsapp or email

-- read at and delivered at timestamps for messages
-- allow user to leave group if not added by admin
-- allow user to record a voice note and send as message
