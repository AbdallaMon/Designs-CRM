ALTER TABLE ClientLead
MODIFY status ENUM(
'NEW',
'IN_PROGRESS',
'INTERESTED',
'NEEDS_IDENTIFIED',
'NEGOTIATING',
'REJECTED',
'FINALIZED',
'CONVERTED',
'ON_HOLD',
'ARCHIVED'
) NOT NULL DEFAULT 'NEW';

ALTER TABLE Task
ADD COLUMN updatedAt DATETIME NULL;

UPDATE Task
SET updatedAt = createdAt
WHERE updatedAt IS NULL;

ALTER TABLE Project
ADD COLUMN isModification BOOLEAN NOT NULL DEFAULT FALSE;
