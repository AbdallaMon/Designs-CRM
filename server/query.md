ALTER TABLE ClientLead
MODIFY COLUMN status
ENUM(
'NEW',
'IN_PROGRESS',
'INTERESTED',
'NEEDS_IDENTIFIED',
'NEGOTIATING',
'REJECTED',
'FINALIZED',
'CONVERTED',
'ON_HOLD',
'ARCHIVED',
'LEADEXCHANGE'
)
NOT NULL DEFAULT 'NEW';

ALTER TABLE `User`
ADD COLUMN `maxLeadCountPerDay` INT NOT NULL DEFAULT 5;
